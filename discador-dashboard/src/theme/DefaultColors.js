// src/theme/DefaultColors.js
import { createTheme } from '@mui/material/styles';

// Exemplo de tema básico
export const baselightTheme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#dc004e',
    },
  },
});
